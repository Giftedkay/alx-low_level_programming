#include "main.h"

/**
 * _islower - checks is alphabet is lower alphabet
 * @c: working operand
 *
 * Return: Returns 1 or 0 depending on the condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
